import React from "react";

import useStyles from "./styles.js";

const Auth = () => {
  const state = null;
  const classes = useStyles();

  return (
    <div>
      <h1>auth</h1>
    </div>
  );
};

export default Auth;
